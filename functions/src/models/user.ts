/*
 * In this folder we are placed all schema structure
 */

export interface userInfo {
    firstName: string;
    lastName: string;
    address: string;
    gender: string;
    mobileNumber: string;
}
  